# Fireside Mobile SDK

Run a Fireside Interview inside your own mobile app.

This repository holds the **licence** and the **install docs**. The SDK itself
is published to each platform's standard package registry.

## Install

| Platform | Where it comes from | Coordinate |
|---|---|---|
| Android | Maven Central | `ai.fireside:sdk-android:<version>` |
| iOS | Swift Package Manager | `https://github.com/fireside-hq/fireside-ios-sdk.git` |
| Flutter | pub.dev | `fireside_flutter: ^<version>` |

Every release carries the same version number on all three platforms.

## Licence

Commercial. See [`LICENSE`](LICENSE).

The Software is available to Fireside customers, in compiled form only. Contact
your Fireside representative for licensing.
